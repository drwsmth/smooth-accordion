# smooth-accordion
An accordion built with ES6 and WAAPI.

#Example use:

```
new Accordion({
	singularMode: true,
	useSchema: true,
	classGroup: '.accordion__group'
})
new Accordion({
	singularMode: false,
	classGroup: '.navigation__sidebar'
});
```
